# Poster Assets

This poster is intentionally self-contained and currently uses inline SVG, CSS, and generated SVG charts instead of external image assets.

If future visual assets are added, keep them local to this directory so the poster can render offline.
